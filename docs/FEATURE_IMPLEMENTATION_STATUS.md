# 功能实现记录

分支：`f-domin1c`。以下记录区分源码实现与实际验收。按用户要求不运行语言检查；未执行的构建、自动化和双 Windows 场景不作为通过记录。

| 编号 | 源码交付 | 验证与遗留 |
| --- | --- | --- |
| T00 | 沿用 WINDOWS_TESTING_GUIDE.md 和研究清单中的双机验收场景 | 未执行双机安装、ACK 故障与离线验收，保持未勾选 |
| T01 | 统一投递状态标签，补充单设备/密钥丢失/本机删除与撤回说明；见 DAILY_CHAT_GUIDE.md | 开发完成待验收；未执行 UI 场景 |
| T02 | 主进程通用通知、按消息去重/会话节流、点击定位、锁屏清理、账号隔离、持久静音与托盘退出 | 开发完成待验收；需实测 Windows 通知中心、100 条补收及退出无残留 |
| T03 | Ctrl+F 分批解密全部本机会话历史、取消、摘要和前后导航；独立上下文接口定位旧消息，共用编辑/撤回投影 | 开发完成待验收；需 10,000 条、损坏密文、快速切换和内存/耗时场景 |

T03 不建立明文索引，每批最多 100 条，结果最多保留 500 条摘要并显示总匹配数；不把扫描结果作为已读。上下文读取前后各 20 条并核对当前账号；本机删除通过同一可见性查询排除，撤回正文由共享投影排除。引用/转发快照沿用既有保留规则。

T02 增加 `conversation_notifications` 表，按账号和联系人存储静音，原库自动建表；不改变消息协议。通知仅对联系人产生，每会话最多 30 秒一次；默认只显示“有新消息”，不发送姓名或正文到通知中心。锁屏期间不显示通知，解锁不补发旧提醒；归档不会自动静音。关闭窗口保留应用运行，托盘退出遵循原有草稿保存保护。
