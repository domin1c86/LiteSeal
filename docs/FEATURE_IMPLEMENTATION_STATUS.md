# P0/P1 源码交付记录

日期：2026-09-22；分支：`f-domin1c`。范围按用户明确回复为 P0/P1；P2/P3 暂缓。

本轮只编写源码、文档和测试用例。未安装依赖、运行语言检查、构建、自动化测试、UI 测试、双机安装或发布部署。“待验收”不是测试通过，研究清单的复选框保持未勾选。实际场景记录入口为 [P0/P1 验收记录](P0_P1_ACCEPTANCE_RECORD.md)。

| 编号 | 本轮交付 | 状态与遗留 | 主提交 |
| --- | --- | --- | --- |
| T00 | 隔离双机验收记录模板，覆盖安装、身份、中文/emoji、断网、ACK 丢失、乱序和晚到变更 | **验收未执行**，需要两台 Windows 和隔离账号 | 本记录提交 |
| T01 | 统一投递状态和单设备/密钥丢失/删除/撤回说明 | 源码完成待验收 | `58be177` |
| T02 | 通用通知、去重/会话节流、点击定位、持久静音、锁屏清理、托盘退出 | 源码完成待验收 | `120b9d0` |
| T03 | Ctrl+F、分批解密历史、取消、摘要、前后导航、旧消息上下文定位 | 源码完成待验收 | `bb32567` |
| T04 | 20 MiB、随机密钥认证加密、1 MiB 分块、进度/停止/重试、另存为、配额与回收 | 源码完成待验收 | `ea72dd0` |
| T05 | PNG/JPEG/WebP 识别、发送前预览/确认、验证后缩略图/大图 | 源码完成待验收；拖拽/粘贴按清单后续处理 | `ea72dd0` 及收尾提交 |
| T06 | 全部/未读/归档、本机自定义列表和持久成员 | 源码完成待验收 | `582311a` |
| T07 | 删除联系人、加密本机备注、完整身份区分 | 源码完成待验收；头像/公开昵称后续处理 | `582311a` |
| T08 | 服务端请求/接受/拒绝/拉黑/解除拉黑，限制正常投递和补收 | 源码完成待验收 | `3a6113c` |
| T09 | emoji、中文 IME 保护、多行输入、Enter/Shift+Enter、Esc、方向键和缩放 | 源码完成待验收 | `10d2509` 及收尾提交 |
| T10 | 独立签名加密回应、按用户版本替换/撤销、持久重试与增量补收 | 源码完成待验收 | `63ac87f` |
| T11 | 收藏引用、加密便笺、原文不可用占位和取消收藏 | 源码完成待验收 | `582311a` |
| T12 | 256 MiB 缓存上限、按会话清理、逻辑/数据库页/实际文件大小区分 | 源码完成待验收 | `ea72dd0` 及收尾提交 |
| T13 | 当前设备/会话列表、退出全部会话、本机断开 | 源码完成待验收 | `3a6113c` |
| T14 | 验证 Windows 密码后启用/关闭、锁屏/空闲/重启锁定、主进程 IPC 门禁 | 源码完成待验收；不支持 Hello PIN | `5b5129e` 及收尾提交 |
| T15 | 固定仓库版本检查、签名发布模式、升级前快照与回滚说明 | 源码/方案完成待验收；缺实际证书签名、安装和回滚记录 | `ab96205` |

## 协议、存储和兼容性

- PostgreSQL 新增迁移 4–6：附件对象/分块、联系人投递策略、回应事件。SQLite 在既有初始化中创建通知、加密整理数据、附件任务和回应表。不删除原消息和数据库。
- 附件在既有签名信封的加密正文中使用 `LiteSeal:2` 格式，外层信封及签名字段不变。Rust 解密出口剥离附件密钥，渲染进程只读文件信息。**试用双方客户端和服务端必须一起升级**，旧客户端不是附件兼容目标；新界面对未知附件版本显示占位。
- 文件使用每对象随机密钥和认证加密；名称、MIME、密钥仅在加密描述内。服务端对象绑定消息编号与收件身份，收件人下载需要已签名消息的持久 receipt。消息 ACK 与文件下载是两个状态。
- 默认对象期限 30 天，孤立对象 24 小时回收。`LITESEAL_ATTACHMENT_DAYS`（1–365）和 `LITESEAL_ATTACHMENT_ORPHAN_HOURS`（1–168）可调整。启动后及每小时清理，到期下载立即拒绝。每账号 512 MiB/2000 对象；本机 256 MiB。清缓存不删正文、链和待发任务。SQLite 空间可复用，不保证文件缩小；界面单独展示数据库和日志文件大小。
- 上传沿用同一对象/消息编号。取消任务不撤回已提交消息，过期暂存提示同任务重新上传。保存仅使用主进程系统对话框选定路径；认证前不写目标文件。图片预览在内存中拼接认证内容，不建立明文预览缓存。
- 搜索每批 100 条，最多保留 500 条结果摘要，显示总匹配/扫描/跳过数。匹配在本机内存中完成，无明文索引。上下文前后各 20 条；撤回/删除不再可搜，引用/转发快照沿用既有保留规则。
- 备注、列表、收藏编号和便笺使用单账号加密整理数据，首版总量 1 MiB。收藏不保存正文快照，每次按消息可见性读取；便笺不发服务端。
- 联系请求最多 100 个待处理身份。接受前不接收新消息密文，由发送方 outbox 保留原消息重试。拒绝/拉黑无正常通知/未读；拉黑前已存离线密文隔离保留以保护消息链，重新接受后恢复。解除拉黑只恢复待处理状态。已收消息不被擦除。撤回仍可补收，被拉黑者的新编辑正文停止投递。**既有联系人升级后需显式允许接收。**
- 回应使用独立签名域 `LiteSeal/reaction/v1`，覆盖目标、身份、设备、版本、密文。每用户维护自己的版本，清空也是事件。服务端不知道 emoji；撤回后的 UI 不显示回应。
- 应用锁可选，启用前先验证 Windows 账号密码，避免无可用密码的设备意外锁定。不支持 Hello PIN，不是全库加密。普通重启读取持久配置并先锁定；Windows 解锁不自动解开应用锁。主进程拦截锁定时的业务 IPC。未启用应用锁时 Windows 锁屏仍抑制通知。

## 验证与遗留

新增 `shared/tests/attachment_reaction_test.rs`，描述篡改/错密钥/截断、空附件和回应签名绑定用例，**未运行**。所有 UI、网络、升级及权限场景见验收模板，均待执行。未使用真实用户数据，没有安装包发布、推送远端或部署。

签名证书、双 Windows 与隔离账号是后续验收环境，不是本轮已获得的结果。不得把源码交付转换成“已验收”，也不在旧开发清单重复宣称通过。
